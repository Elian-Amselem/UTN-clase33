const path = require("path");

// Que queremos crear automaticamente
module.exports = {
    // archivo de configuracion para la conexión a la BD
    config: path.resolve('./src/database/config', 'config.js'),
    'models-path': path.resolve('./src/database/models')
}

// Ahora lo ejecutamos con:
//
//  sequelize init

// Pero si no les reconoce el comando
// Lo tenemos que instalar de forma global 
//
//  npm i -g sequelize-cli